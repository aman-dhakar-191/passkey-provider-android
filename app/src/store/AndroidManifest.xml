<?xml version="1.0" encoding="utf-8"?>
<!-- Store builds (Google Play, Indus Appstore): the store delivers updates, so the self-updater's
     permissions and install callback are removed. Its code stays but is never started (SELF_UPDATE=false). -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission
        android:name="android.permission.REQUEST_INSTALL_PACKAGES"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.POST_NOTIFICATIONS"
        tools:node="remove" />

    <application>
        <receiver
            android:name="io.github.amandhakar.passkey.update.InstallResultReceiver"
            tools:node="remove" />
    </application>
</manifest>
